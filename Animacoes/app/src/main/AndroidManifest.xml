<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dominando.android.animacoes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Animacoes">
        <activity
            android:name=".RevealActivity"
            android:exported="true" />
        <activity
            android:name=".TransitionActivity"
            android:exported="true" />
        <activity
            android:name=".LayoutChangesActivity"
            android:exported="true" />
        <activity
            android:name=".SpriteActivity"
            android:exported="true" />
        <activity
            android:name=".PropertyAnimationsActivity"
            android:exported="true" />
        <activity
            android:name=".ViewAnimationsActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>